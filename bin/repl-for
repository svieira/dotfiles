#!/usr/bin/env bash

printf "REPL for %s\n" "$@"

notblank() {
  [ $# -gt 0 ]
}

while true ; do
  printf "%s> " "$@"
  read -r || break;
  notblank $REPLY || continue;
  [[ "$REPLY" == "exit" ]] && break
  eval command \"\$@\" "$REPLY"
done